# P7 管理台 Implementation Plan

> REQUIRED SUB-SKILL: superpowers:executing-plans（Inline）。

**Goal:** 管理台：后端 `/admin/*`（管理员鉴权）提供统计/用户/日志/上游 Key 管理；前端 Next.js（App Router）登录后查看四模块。

**Architecture:** 后端 `admins`/`upstream_keys` 表 + 管理员 JWT（`scope:admin`）+ `admin_required` 依赖；只读聚合端点（stats/users/errors/events）+ keys CRUD（响应脱敏，绝不回完整 Key）。前端 `admin/`（Next.js 14 App Router，**精简版**：原生 fetch + 轻组件，不引 shadcn/Tremor 以降低安装面与构建风险，记为偏差，后续可叠加）；token 存 localStorage；登录后访问 dashboard/users/logs/keys。

**网络：** pypi/npm 直连被墙——后端 uv 已用清华镜像；前端 pnpm 用 npmmirror.com（`admin/.npmrc`）。

**约定：** commit 中文 + Co-Authored-By；后端 `uv run`，pytest 与 commit 分开。

---

## Part A — 后端 /admin API

### Task 1: admins / upstream_keys 模型 + 迁移 + 建管理员脚本

**Files:** `server/app/db/models.py`、迁移、`server/tests/conftest.py`、`server/scripts/create_admin.py`(新)

- [ ] **Step 1: 模型**（末尾）
```python
class Admin(Base):
    __tablename__ = "admins"
    id: Mapped[int] = mapped_column(BigInteger, primary_key=True, autoincrement=True)
    email: Mapped[str] = mapped_column(String(255), unique=True, nullable=False)
    password_hash: Mapped[str] = mapped_column(String(255), nullable=False)
    role: Mapped[str] = mapped_column(String(32), default="admin", nullable=False)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now(), nullable=False)


class UpstreamKey(Base):
    """上游 DeepSeek Key 池（MVP：明文存库，响应脱敏只回末 4 位；加密存储留后续硬化）。"""
    __tablename__ = "upstream_keys"
    id: Mapped[int] = mapped_column(BigInteger, primary_key=True, autoincrement=True)
    label: Mapped[str] = mapped_column(String(64), nullable=False)
    key_value: Mapped[str] = mapped_column(String(255), nullable=False)
    status: Mapped[str] = mapped_column(String(16), default="active", nullable=False)  # active|disabled
    used_tokens: Mapped[int] = mapped_column(BigInteger, default=0, nullable=False)
    balance_note: Mapped[str | None] = mapped_column(String(255), nullable=True)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now(), nullable=False)
```
- [ ] **Step 2: 迁移** `uv run alembic revision --autogenerate -m "admins_upstream_keys" && uv run alembic upgrade head`
- [ ] **Step 3: conftest TRUNCATE 加 admins, upstream_keys**
- [ ] **Step 4: 建管理员脚本** `server/scripts/create_admin.py`
```python
"""建管理员：uv run python scripts/create_admin.py <email> <password>"""
import asyncio
import sys

from app.core.security import hash_password
from app.db.base import async_session
from app.db.models import Admin


async def main(email: str, password: str) -> None:
    async with async_session() as s:
        s.add(Admin(email=email.strip().lower(), password_hash=hash_password(password)))
        await s.commit()
    print("created admin", email)


if __name__ == "__main__":
    asyncio.run(main(sys.argv[1], sys.argv[2]))
```
- [ ] **Step 5: Commit** — `P7: admins/upstream_keys 模型 + 迁移 + 建管理员脚本`

### Task 2: 管理员 token + admin_required + /admin/login

**Files:** `server/app/core/security.py`、`server/app/routers/admin.py`(新)、`server/app/main.py`、`server/tests/test_admin_auth.py`(新)

- [ ] **Step 1: security 加管理员 token**
```python
def create_admin_token(admin_id: int, ttl_seconds: int | None = None) -> str:
    ttl = ttl_seconds if ttl_seconds is not None else settings.access_ttl_min * 60
    now = int(time.time())
    return jwt.encode({"sub": str(admin_id), "scope": "admin", "iat": now, "exp": now + ttl}, settings.jwt_secret, algorithm=_ALG)


def decode_admin_token(token: str) -> int | None:
    try:
        p = jwt.decode(token, settings.jwt_secret, algorithms=[_ALG])
        return int(p["sub"]) if p.get("scope") == "admin" else None
    except (jwt.InvalidTokenError, KeyError, ValueError):
        return None
```
- [ ] **Step 2: 写失败测试** `server/tests/test_admin_auth.py`
```python
import httpx
from httpx import ASGITransport
from app.main import app
from app.core.security import hash_password
from app.db.models import Admin


def _c():
    return httpx.AsyncClient(transport=ASGITransport(app=app), base_url="http://test")


async def test_admin_login_and_guard(db_session):
    db_session.add(Admin(email="adm@x.com", password_hash=hash_password("adminpw")))
    await db_session.commit()
    async with _c() as c:
        # 未带 token → 401
        assert (await c.get("/admin/stats")).status_code == 401
        # 登录
        r = await c.post("/admin/login", json={"email": "adm@x.com", "password": "adminpw"})
        assert r.status_code == 200
        tok = r.json()["token"]
        # 带 token → 200
        r2 = await c.get("/admin/stats", headers={"Authorization": f"Bearer {tok}"})
        assert r2.status_code == 200


async def test_admin_login_bad_pw(db_session):
    db_session.add(Admin(email="adm@x.com", password_hash=hash_password("adminpw")))
    await db_session.commit()
    async with _c() as c:
        assert (await c.post("/admin/login", json={"email": "adm@x.com", "password": "no"})).status_code == 401
```
- [ ] **Step 3: 跑（红）**
- [ ] **Step 4: 写 admin 路由**（login + admin_required + stats 占位先让测试过）`server/app/routers/admin.py`
```python
from typing import AsyncIterator

from fastapi import APIRouter, Depends, HTTPException, Request
from pydantic import BaseModel
from sqlalchemy import func, select
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.security import create_admin_token, decode_admin_token, verify_password
from app.db.base import async_session
from app.db.models import Admin, DailyUsage, ErrorLog, Event, UpstreamKey, User

router = APIRouter(prefix="/admin")


async def get_session() -> AsyncIterator[AsyncSession]:
    async with async_session() as s:
        yield s


def admin_required(request: Request) -> int:
    auth = request.headers.get("authorization", "")
    admin_id = decode_admin_token(auth[7:].strip()) if auth.lower().startswith("bearer ") else None
    if admin_id is None:
        raise HTTPException(status_code=401, detail="需要管理员登录")
    return admin_id


class LoginIn(BaseModel):
    email: str
    password: str


@router.post("/login")
async def admin_login(body: LoginIn, session: AsyncSession = Depends(get_session)):
    admin = await session.scalar(select(Admin).where(Admin.email == body.email.strip().lower()))
    if admin is None or not verify_password(body.password, admin.password_hash):
        raise HTTPException(status_code=401, detail="邮箱或密码错误")
    return {"token": create_admin_token(admin.id), "email": admin.email}


@router.get("/stats")
async def stats(session: AsyncSession = Depends(get_session), _: int = Depends(admin_required)):
    users = await session.scalar(select(func.count()).select_from(User)) or 0
    translations = await session.scalar(
        select(func.count()).select_from(Event).where(Event.type == "translate_done")
    ) or 0
    errors = await session.scalar(select(func.count()).select_from(ErrorLog)) or 0
    tokens = await session.scalar(select(func.coalesce(func.sum(DailyUsage.input_tokens + DailyUsage.output_tokens), 0))) or 0
    top_rows = (await session.execute(
        select(Event.host, func.count().label("n")).where(Event.host.is_not(None))
        .group_by(Event.host).order_by(func.count().desc()).limit(10)
    )).all()
    return {
        "users": int(users), "translations": int(translations), "errors": int(errors),
        "tokens": int(tokens), "topHosts": [{"host": h, "count": int(n)} for h, n in top_rows],
    }
```
`main.py` 挂载 `admin.router`。
- [ ] **Step 5: 跑（绿）**
- [ ] **Step 6: Commit** — `P7: 管理员 token + /admin/login + 鉴权守卫 + /admin/stats`

### Task 3: /admin/users · /admin/errors · /admin/events

**Files:** `server/app/routers/admin.py`、`server/tests/test_admin_data.py`(新)

- [ ] **Step 1: 写失败测试**（含数据：建 user + daily_usage + events + error_logs，断言列表返回）`server/tests/test_admin_data.py`（用 db_session 造数据 + admin token；helper 复用 test_admin_auth 的登录或直接造 token）。
```python
import httpx
from httpx import ASGITransport
from app.main import app
from app.core.security import create_admin_token, hash_password
from app.db.models import Admin, ErrorLog, Event, User
from app.services.usage_repo import DailyUsageRepo


async def _admin_token(db_session):
    a = Admin(email="a@x.com", password_hash=hash_password("pw"))
    db_session.add(a); await db_session.commit(); await db_session.refresh(a)
    return create_admin_token(a.id)


async def test_users_and_logs(db_session):
    tok = await _admin_token(db_session)
    u = User(email="u@x.com", password_hash="x"); db_session.add(u); await db_session.commit(); await db_session.refresh(u)
    await DailyUsageRepo(db_session).add(u.id, "2026-06-08", 100, 50, pages=2)
    db_session.add(Event(type="translate_done", host="react.dev", props={"blocks": 9}))
    db_session.add(ErrorLog(kind="network", message="boom", context={}))
    await db_session.commit()
    h = {"Authorization": f"Bearer {tok}"}
    async with httpx.AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as c:
        users = (await c.get("/admin/users", headers=h)).json()
        assert any(x["email"] == "u@x.com" for x in users)
        errors = (await c.get("/admin/errors", headers=h)).json()
        assert errors and errors[0]["kind"] == "network"
        events = (await c.get("/admin/events", headers=h)).json()
        assert events and events[0]["type"] == "translate_done"
```
- [ ] **Step 2: 跑（红）**
- [ ] **Step 3: 实现** 在 admin.py 加（用今日聚合 token；users 关联 daily_usage 当日 + quota_tier）：
```python
from datetime import date
from app.db.models import QuotaTier

@router.get("/users")
async def users(session: AsyncSession = Depends(get_session), _: int = Depends(admin_required)):
    today = date.today().isoformat()
    rows = (await session.execute(select(User).order_by(User.id.desc()).limit(200))).scalars().all()
    out = []
    for u in rows:
        du = await session.scalar(select(DailyUsage).where(DailyUsage.user_id == u.id, DailyUsage.local_date == today))
        qt = await session.get(QuotaTier, u.id)
        out.append({
            "id": u.id, "email": u.email,
            "tokensToday": int((du.input_tokens + du.output_tokens) if du else 0),
            "tier": qt.tier if qt else 0,
            "createdAt": u.created_at.isoformat() if u.created_at else None,
        })
    return out


@router.get("/errors")
async def errors(session: AsyncSession = Depends(get_session), _: int = Depends(admin_required)):
    rows = (await session.execute(select(ErrorLog).order_by(ErrorLog.id.desc()).limit(200))).scalars().all()
    return [{"id": r.id, "ts": r.ts.isoformat(), "kind": r.kind, "message": r.message, "context": r.context, "userId": r.user_id} for r in rows]


@router.get("/events")
async def events(session: AsyncSession = Depends(get_session), _: int = Depends(admin_required)):
    rows = (await session.execute(select(Event).order_by(Event.id.desc()).limit(200))).scalars().all()
    return [{"id": r.id, "ts": r.ts.isoformat(), "type": r.type, "host": r.host, "props": r.props} for r in rows]
```
- [ ] **Step 4: 跑（绿）**
- [ ] **Step 5: Commit** — `P7: /admin/users · /admin/errors · /admin/events`

### Task 4: /admin/keys CRUD（上游 Key，脱敏）

**Files:** `server/app/routers/admin.py`、`server/tests/test_admin_keys.py`(新)

- [ ] **Step 1: 写失败测试**
```python
async def test_keys_crud_masked(db_session):
    tok = await _admin_token(db_session)
    h = {"Authorization": f"Bearer {tok}"}
    async with httpx.AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as c:
        r = await c.post("/admin/keys", json={"label": "main", "key": "sk-abcdef123456"}, headers=h)
        assert r.status_code == 200 and r.json()["masked"].endswith("3456") and "abcdef" not in r.json()["masked"]
        kid = r.json()["id"]
        lst = (await c.get("/admin/keys", headers=h)).json()
        assert any(k["id"] == kid for k in lst) and all("key_value" not in k for k in lst)
        r2 = await c.patch(f"/admin/keys/{kid}", json={"status": "disabled"}, headers=h)
        assert r2.status_code == 200 and r2.json()["status"] == "disabled"
```
（复用 `_admin_token`——把它移到一个共享 helper 或在本文件再定义一份。)
- [ ] **Step 2: 跑（红）**
- [ ] **Step 3: 实现**
```python
def _mask(v: str) -> str:
    return ("•" * max(0, len(v) - 4)) + v[-4:] if v else ""

class KeyIn(BaseModel):
    label: str
    key: str

class KeyPatch(BaseModel):
    status: str

def _key_out(k: UpstreamKey) -> dict:
    return {"id": k.id, "label": k.label, "masked": _mask(k.key_value), "status": k.status,
            "usedTokens": k.used_tokens, "balanceNote": k.balance_note}

@router.get("/keys")
async def list_keys(session: AsyncSession = Depends(get_session), _: int = Depends(admin_required)):
    rows = (await session.execute(select(UpstreamKey).order_by(UpstreamKey.id))).scalars().all()
    return [_key_out(k) for k in rows]

@router.post("/keys")
async def add_key(body: KeyIn, session: AsyncSession = Depends(get_session), _: int = Depends(admin_required)):
    k = UpstreamKey(label=body.label, key_value=body.key)
    session.add(k); await session.commit(); await session.refresh(k)
    return _key_out(k)

@router.patch("/keys/{key_id}")
async def patch_key(key_id: int, body: KeyPatch, session: AsyncSession = Depends(get_session), _: int = Depends(admin_required)):
    k = await session.get(UpstreamKey, key_id)
    if k is None:
        raise HTTPException(status_code=404, detail="not found")
    k.status = body.status
    await session.commit()
    return _key_out(k)
```
- [ ] **Step 4: 跑（绿）+ 全量后端**
- [ ] **Step 5: Commit** — `P7: /admin/keys CRUD（上游 Key，响应脱敏）`

---

## Part B — Next.js 管理台前端

### Task 5: 脚手架 + 登录

**Files:** `admin/`（package.json/.npmrc/tsconfig/next.config/.gitignore/app/lib）

- [ ] **Step 1: 建工程**
```bash
mkdir -p /Users/eric/Desktop/projects/immersive-translate/admin && cd /Users/eric/Desktop/projects/immersive-translate/admin
printf 'registry=https://registry.npmmirror.com\n' > .npmrc
```
写 `admin/package.json`：
```json
{
  "name": "imt-admin",
  "private": true,
  "scripts": { "dev": "next dev -p 3001", "build": "next build", "start": "next start -p 3001", "typecheck": "tsc --noEmit" },
  "dependencies": { "next": "^14.2.5", "react": "^18.3.1", "react-dom": "^18.3.1" },
  "devDependencies": { "typescript": "^5.6.3", "@types/react": "^18.3.12", "@types/node": "^22" }
}
```
`admin/.gitignore`：`node_modules/\n.next/\n`。
`admin/tsconfig.json`（Next 标准）+ `admin/next.config.mjs`（`export default {}`）。
`admin/.env.local`（gitignore）：`NEXT_PUBLIC_API=http://localhost:8000`。
然后 `pnpm install`（用 npmmirror）。
- [ ] **Step 2: API 客户端 + 登录页**
  - `admin/lib/api.ts`：读 `localStorage.imt_admin_token`，`api(path, opts)` 自动带 `Authorization`，base 用 `process.env.NEXT_PUBLIC_API`。
  - `admin/app/layout.tsx`：根布局 + 顶部导航（dashboard/users/logs/keys/登出）。
  - `admin/app/login/page.tsx`（`'use client'`）：邮箱密码表单 → `POST /admin/login` → 存 token → 跳 `/`。
- [ ] **Step 3: typecheck** `pnpm typecheck`
- [ ] **Step 4: Commit** — `P7 前端: Next.js 脚手架 + 登录`

### Task 6: 四模块页面

**Files:** `admin/app/page.tsx`(dashboard)、`app/users/page.tsx`、`app/logs/page.tsx`、`app/keys/page.tsx`

- [ ] **Step 1:** 各页 `'use client'`，挂载时 `api()` 取数据，渲染：
  - dashboard：`/admin/stats` → 几个统计卡（users/translations/errors/tokens）+ Top 域名表。
  - users：`/admin/users` → 表格（email/今日 token/档位/注册时间）。
  - logs：`/admin/errors` → 表格（时间/类型/消息）。
  - keys：`/admin/keys` → 表格（label/掩码/状态）+ 添加表单 + 启停按钮（PATCH）。
  - 无 token 时跳 `/login`。
- [ ] **Step 2: build** `pnpm build`（Next 构建即类型检查 + 产出，作为验证）
- [ ] **Step 3: Commit** — `P7 前端: dashboard/users/logs/keys 四模块`

### Task 7: 端到端验证 + 合并

- [ ] **Step 1: 后端全量** `cd server && uv run pytest -q`（全绿）
- [ ] **Step 2: 前端 build** `cd admin && pnpm build`（成功）
- [ ] **Step 3:（可选）联调** 起后端 + `uv run python scripts/create_admin.py admin@imt.local adminpw` 建管理员；`pnpm dev` 起前端；浏览器 :3001 登录看四模块（localhost 在调试 Chrome 内可达）。
- [ ] **Step 4: 合并 main，删分支。**

---

## Self-Review
- 覆盖设计 P7：日志查看（errors）、打点统计（stats/events）、用户管理（users + tier + 今日 token）、API Key 管理（upstream_keys，脱敏）。管理员独立鉴权。
- 偏差：前端用精简原生组件而非 shadcn/Tremor（降安装/构建风险，autonomous 无浏览器逐帧验证）；功能完整，样式后续可叠加。记入开放项。
- 安全：上游 Key 明文存库 + 响应脱敏（绝不回完整 Key）；加密存储留硬化。管理员 token 用 scope:admin 区分终端用户 token。
- 网络：前端依赖走 npmmirror（npm 直连被墙）。
